name: Build APK
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-pip build-essential git python3-dev python3-setuptools python3-venv libffi-dev libssl-dev zip unzip openjdk-8-jdk
      
      - name: Install Buildozer
        run: |
          pip install buildozer==1.5.0 cython==0.29.33
      
      - name: Build APK
        run: |
          yes | buildozer android debug
      
      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: app-debug
          path: bin/*.apk
